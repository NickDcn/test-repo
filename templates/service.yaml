apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-repo.git.fullname" . }}
  labels:
    {{- include "test-repo.git.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-repo.git.selectorLabels" . | nindent 4 }}
